---
description: Agents Glass tint is the goal (sidebar section + agent-panel); IDE borders out of scope; plugins cannot color Agents DOM yet
alwaysApply: true
---

# CCC workspace visual identity

Goal surfaces (same accent per project): Agents `glass-sidebar-workspace-section-root` + `agent-panel` — **not** the IDE.

1. Name the accent in replies (Amber / Cyan + hex).
2. Never claim those Agents regions changed color via this plugin — Cursor exposes no API for `--glass-chat-surface-background` / section tint yet.
3. Do not treat IDE `sideBar.border` apply as the solution for this vorhaben.
